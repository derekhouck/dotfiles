[user]
	email = derek@derekhouck.com
	name = Derek Houck\n
[push]
	default = simple
[core]
	editor = mvim -f
	excludesfile = ~/.gitignore_global
[credential]
	helper = osxkeychain
[alias]
	co = checkout
	lg = log --oneline --all --graph --decorate
	up = "!git remote update -p; git merge --ff-only @{u}"
[color]
	ui = auto
[merge]
	tool = code
